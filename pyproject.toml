[tool.poetry]
name = "icecream-dataops"
version = "0.1.0"
description = ""
authors = ["snandy"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
cognite-toolkit = "0.5.30"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
